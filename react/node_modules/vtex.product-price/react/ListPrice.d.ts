import { CssHandlesTypes } from 'vtex.css-handles';
declare const CSS_HANDLES: readonly ["listPrice", "listPriceValue", "listPriceWithTax", "listPriceWithUnitMultiplier", "taxPercentage", "taxValue", "unitMultiplier", "measurementUnit"];
interface Props {
    message?: string;
    markers?: string[];
    /** Used to override default CSS handles */
    classes?: CssHandlesTypes.CustomClasses<typeof CSS_HANDLES>;
    alwaysShow?: boolean;
}
declare function ListPrice({ message, markers, classes, alwaysShow, }: Props): JSX.Element | null;
declare namespace ListPrice {
    var schema: {
        title: string;
    };
}
export default ListPrice;
