import { CssHandlesTypes } from 'vtex.css-handles';
import { CSS_HANDLES } from './components/InstallmentsRenderer';
interface Props {
    message?: string;
    markers?: string[];
    /** Used to override default CSS handles */
    classes?: CssHandlesTypes.CustomClasses<typeof CSS_HANDLES>;
}
declare function InstallmentsListItem({ message, markers, classes, }: Props): JSX.Element | null;
declare namespace InstallmentsListItem {
    var schema: {
        title: string;
    };
}
export default InstallmentsListItem;
