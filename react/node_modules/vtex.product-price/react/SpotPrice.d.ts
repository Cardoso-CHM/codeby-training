import { CssHandlesTypes } from 'vtex.css-handles';
declare const CSS_HANDLES: readonly ["spotPrice", "spotPriceValue"];
interface Props {
    message?: string;
    markers?: string[];
    /** Used to override default CSS handles */
    classes?: CssHandlesTypes.CustomClasses<typeof CSS_HANDLES>;
    alwaysShow?: boolean;
}
declare function SpotPrice({ message, markers, classes, alwaysShow, }: Props): JSX.Element | null;
declare namespace SpotPrice {
    var schema: {
        title: string;
    };
}
export default SpotPrice;
