import { CssHandlesTypes } from 'vtex.css-handles';
import { CSS_HANDLES } from './components/InstallmentsRenderer';
interface Props {
    message?: string;
    markers?: string[];
    installmentsCriteria?: 'max-quantity' | 'max-quantity-without-interest';
    installmentOptionsFilter?: {
        paymentSystemName?: string;
        installmentsQuantity?: number;
    };
    /** Used to override default CSS handles */
    classes?: CssHandlesTypes.CustomClasses<typeof CSS_HANDLES>;
}
declare function Installments({ message, markers, installmentsCriteria, installmentOptionsFilter, classes, }: Props): JSX.Element | null;
declare namespace Installments {
    var schema: {
        title: string;
    };
}
export default Installments;
