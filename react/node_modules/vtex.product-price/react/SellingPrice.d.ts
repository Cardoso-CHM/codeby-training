import { CssHandlesTypes } from 'vtex.css-handles';
declare const CSS_HANDLES: readonly ["sellingPrice", "sellingPriceValue", "sellingPriceWithTax", "sellingPriceWithUnitMultiplier", "taxPercentage", "taxValue", "measurementUnit", "unitMultiplier"];
interface Props {
    message?: string;
    markers?: string[];
    /** Used to override default CSS handles */
    classes?: CssHandlesTypes.CustomClasses<typeof CSS_HANDLES>;
    alwaysShow?: boolean;
}
declare function SellingPrice({ message, markers, classes, alwaysShow, }: Props): JSX.Element | null;
declare namespace SellingPrice {
    var schema: {
        title: string;
    };
}
export default SellingPrice;
