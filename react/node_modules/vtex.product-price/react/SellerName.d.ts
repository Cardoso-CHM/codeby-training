import { CssHandlesTypes } from 'vtex.css-handles';
declare const CSS_HANDLES: readonly ["sellerNameContainer", "sellerName"];
interface Props {
    message?: string;
    markers?: string[];
    /** Used to override default CSS handles */
    classes?: CssHandlesTypes.CustomClasses<typeof CSS_HANDLES>;
}
declare function ProductSellerName({ message, markers, classes, }: Partial<Props>): JSX.Element | null;
declare namespace ProductSellerName {
    var schema: {
        title: string;
    };
}
export default ProductSellerName;
