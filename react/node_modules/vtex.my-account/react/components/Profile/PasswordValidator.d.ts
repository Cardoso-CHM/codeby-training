import { Component } from 'react';
declare class PasswordValidator extends Component<Props> {
    state: {
        has8chars: boolean;
        hasNumber: boolean;
        hasCaps: boolean;
        hasLow: boolean;
    };
    componentDidUpdate(prevProps: Props): void;
    render(): JSX.Element;
}
interface Props {
    password: string;
    onValidationChange: (args: {
        valid: boolean;
    }) => void;
}
export default PasswordValidator;
